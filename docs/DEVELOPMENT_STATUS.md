# v2.12.4 开发交接状态

更新时间：2026-08-20

## 版本与安全基线

- 当前开发分支：`upgrade/v2.12.4`
- 本轮开发前稳定备份分支：`backup/v2.12.3-stable`（提交 `9f568b1`）
- 当前候选版：`2.12.4` / versionCode 89
- v2.12.3 正式签名 APK 与完整交付资料仍保存在项目外层 `交付成品/` 和 `交付记录/`，可随时回退。
- 本轮没有访问 GitHub，也没有修改代理、端口、DNS 或其他系统网络配置。

## v2.12.4 调整内容

- 仅在启用自定义背景时适度提高课程卡片透明度：浅色课程色由 82% 调整为 74% 不透明，深色课程色由 96% 调整为 86% 不透明。
- 背景图片在课程卡片下方更容易辨认，同时保留原文字颜色、渐变高光、边框、阴影、当前课程强调及点击区域。
- 未启用自定义背景时继续使用 v2.12.3 的卡片透明度，默认课表观感不变。

## 数据与兼容性

- `tables.json` 仍为 schemaVersion 4，没有新增、删除或修改用户数据字段。
- `preferences.json` 仍为 schemaVersion 4，没有偏好迁移。
- applicationId 与签名证书保持不变，可从 v2.12.3 直接覆盖升级。
- 应用仍不声明 `INTERNET` 或媒体库广泛读取权限，系统云备份与设备迁移数据提取继续关闭。

## 验证结果

- JVM 回归测试 111 项全部通过，0 失败、0 错误、0 跳过。
- API 34 UI 测试 16 项全部通过，0 失败、0 跳过。
- Release/R8 与资源压缩构建成功。
- Release Lint 为 0 错误、17 条警告和 1 条提示；均为既有非阻断项。
- API 34 可视化核验通过：自定义背景可明显透过课程卡片，课程名称、教师和教室仍清晰可读，状态栏全屏背景及安全间距继续正常。
- 从正式签名 v2.12.3（versionCode 88）覆盖安装正式签名 v2.12.4（versionCode 89）成功。
- 连续 3 次冷启动均成功进入 `MainActivity`，崩溃缓冲区为 0 行。
- 正式 APK 已完成 v2/v3 签名校验；签名证书 SHA-256 为 `23410387a26c9ca5b9a1552ba06be642d905fd07d2791bc3bf4db9461ba7d132`，与历史正式版一致。
- 正式 APK SHA-256：`3B59DA31F760AA4B314C1D124F17437C4F2CE4ED34C83592869DBC3659496D5E`；大小 6,769,135 字节。

## 交付边界

- 最终签名 APK、SHA-256、安装测试说明、审计结果、源码快照、界面验收截图和本文件副本统一放在项目外层的 v2.12.4 交付目录。
- 签名材料不复制进仓库、不进入源码快照或交付记录。
- 不自动推送 GitHub；仅在用户之后明确要求时执行公开发布流程。
