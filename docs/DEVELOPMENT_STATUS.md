# v2.12.2 开发交接状态

更新时间：2026-08-20

## 版本与安全基线

- 当前开发分支：`upgrade/v2.12.2`
- 本轮开发前稳定备份分支：`backup/v2.12.1-stable`（提交 `0fcfedb`）
- 当前候选版：`2.12.2` / versionCode 87
- v2.12.1 正式签名 APK 与完整交付资料仍保存在项目外层 `交付成品/` 和 `交付记录/`，可随时回退。
- 本轮没有访问 GitHub，也没有修改代理、端口、DNS 或其他系统网络配置。

## v2.12.2 等价代码清理

- 通过全源码引用统计、Kotlin 强制重编译和 Release/R8 使用报告交叉确认后，删除 5 个没有生产调用的旧入口。
- 删除 `CourseRepository` 中已经被 ViewModel 的完整课表删除规则替代的方法，避免两套业务规则并存。
- 删除 3 个旧版时间解析包装和 1 个未接入界面的文本列预览入口；当前严格解析方法、文本导入和旧数据兼容逻辑完整保留。
- 页面枚举恢复由 `values()` 改为缓存的 `entries`，避免每次读取页面状态时创建临时枚举数组。
- 本轮不改界面、交互、课程计算、提醒、小组件、背景、备份、导入导出或返回逻辑。

## 数据与兼容性

- `tables.json` 仍为 schemaVersion 4，没有新增、删除或修改用户数据字段。
- `preferences.json` 仍为 schemaVersion 4，没有偏好迁移。
- applicationId 与签名证书保持不变，可从 v2.12.1 直接覆盖升级。
- 应用仍不声明 `INTERNET` 或媒体库广泛读取权限，系统云备份与设备迁移数据提取继续关闭。

## 验证结果

- JVM 回归测试 111 项全部通过，0 失败、0 错误、0 跳过。
- API 34 UI 测试 16 项全部通过，0 失败、0 跳过；课程详情返回、日程返回、设置层级及滚动恢复等测试继续通过。
- Kotlin 强制重编译无源码警告；清理后的 Release/R8 与资源压缩构建通过。
- Release Lint 为 0 错误、17 条警告和 1 条提示；均为既有非阻断项。
- 从正式签名 v2.12.1（versionCode 86）覆盖安装正式签名 v2.12.2（versionCode 87）成功。
- 连续 3 次冷启动均进入主界面，崩溃日志为空。
- 正式 APK 已完成 v2/v3 签名校验；签名证书 SHA-256 为 `23410387a26c9ca5b9a1552ba06be642d905fd07d2791bc3bf4db9461ba7d132`，与历史正式版一致。
- 正式 APK SHA-256：`7A0ED334D111B8AFC8848A8B76917E603FB40626DBCAC1A6C9BC6579734CF450`；大小 6,769,135 字节，与 v2.12.1 相同。

## 交付边界

- 最终签名 APK、SHA-256、安装测试说明、审计结果、源码快照和本文件副本统一放在项目外层的 v2.12.2 交付目录。
- 签名材料不复制进仓库、不进入源码快照或交付记录。
- 不自动推送 GitHub；仅在用户之后明确要求时执行公开发布流程。
