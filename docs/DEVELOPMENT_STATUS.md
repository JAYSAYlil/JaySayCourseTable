# v2.8.0 开发交接状态

更新时间：2026-08-20

## 版本与分支

- 当前开发分支：`upgrade/v2.8.0`
- 基础稳定版：`v2.7.0` / versionCode 80
- 当前本地签名交付版：`2.8.0` / versionCode 81
- DeepSeek 接手提交：`df01d9c`、`04229b3`
- 本轮在 DeepSeek 功能基础上完成独立审查、修复、文档和发布级回归。

## 已完成能力

- 本地上课提醒、重启/升级/时间变化恢复、旧闹钟取消和接收端二次校验。
- 桌面小组件、深浅色资源和上课/下课/跨日边界刷新。
- 停课周、iCalendar 导出、网页文本粘贴导入和数据诊断。
- 导入暂存跨 Activity 重建保留；进程重建无暂存数据时安全返回主界面。
- 脱敏与完整备份使用独立系统文件回调，防止重建后安全级别串换。
- 课表 Canvas 绘制优化，同时恢复 TalkBack 对具体空白节次的操作能力。
- 深色空状态可见度、停课周多选刷新、跨节结束时间和当前课程结束边界修复。

## 本轮验证结果

- JVM：67 项，0 失败。
- API 34 模拟器：8 项 Compose/Activity UI 测试，0 失败。
- 强制停止并冷启动 3 次，均成功。
- Debug APK 和启用 R8/资源收缩的 unsigned Release APK 构建成功。
- Release Lint：0 error；剩余提示仅为可选依赖升级和 Apache POI 自带、应用无法联网触发的静态 TLS 检查。
- 发布前审计通过：Manifest 无 `INTERNET`，系统自动备份关闭，Git 未跟踪密钥、真实课表、备份或 APK。
- API 34 实机渲染尺寸下复核深浅色空状态；深色标题对比度问题已修复后复测。
- 正式 APK 已使用历史兼容密钥签名，APK Signature Scheme v2/v3 与证书指纹校验通过。
- API 34 模拟器从正式签名 v2.7.0（80）直接覆盖升级到 v2.8.0（81）成功；正式包连续冷启动 3 次均正常且无崩溃日志。

## 后续操作边界

- 正式签名 APK 已保存到项目根目录的 `交付成品/`；尚未创建 GitHub Release、未推送远端，也未修改任何代理、端口、DNS 或系统网络配置。
- 下一步应由用户在自己的手机上覆盖安装 v2.8.0，使用真实课表完成最终回归。
- 只有在用户明确确认后，才提交/推送并发布 GitHub Release。
- 发布前再次运行 `scripts/pre-release-audit.ps1`，并确认 Git 工作区只包含本版本预期改动。
