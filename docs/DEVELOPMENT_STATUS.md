# v2.12.6 开发交接状态

更新时间：2026-08-20

## 版本与安全基线

- 当前开发分支：`upgrade/v2.12.4`
- 当前候选版：`2.12.6` / versionCode 91
- 本轮没有新建备份分支或重复项目副本；现有 Git 历史仍可用于版本追溯。
- 本轮没有访问 GitHub，也没有修改代理、端口、DNS 或其他系统网络配置。

## v2.12.6 调整内容

- 恢复一级页面之间原有的方向性滑入/滑出动画和课表周次切换动画。
- 仅课程详情页返回课表一级界面或日程来源页时使用 150/90 ms 短淡化，避免详情返回产生明显缩放、位移。
- 保留统一 edge-to-edge 系统栏坐标、默认/自定义背景一致的内容位置和自定义背景全屏覆盖。
- 保留窗口底层、加载阶段和导航容器的当前主题底色，深色模式转场不会透出白色。
- 不改变课程卡片、桌面小组件、课表数据字段和已有功能。

## 数据与兼容性

- `tables.json` 仍为 schemaVersion 4，`preferences.json` 仍为 schemaVersion 4。
- applicationId 与签名证书保持不变，可从 v2.12.5 直接覆盖升级。
- 应用仍不声明 `INTERNET` 或媒体库广泛读取权限，系统云备份与设备迁移数据提取继续关闭。
- 正式 Release 包保持不可调试。

## 验证结果

- JVM 回归测试 111 项全部通过，0 失败、0 错误、0 跳过。
- API 34 UI 测试 16 项全部通过，0 失败、0 错误、0 跳过。
- Release/R8 与资源压缩构建成功。
- Release Lint 为 0 错误、18 条非阻断警告。
- 自动化测试覆盖页面返回来源、滚动位置恢复、深色主题和课表交互；动画恢复没有破坏既有测试。
- 从正式签名 v2.12.5（versionCode 90）覆盖安装正式签名 v2.12.6（versionCode 91）成功。
- 连续 3 次冷启动均成功进入 `MainActivity`，崩溃缓冲区为 0 行。
- 正式 APK 已完成 v2/v3 签名校验；签名证书 SHA-256 为 `23410387a26c9ca5b9a1552ba06be642d905fd07d2791bc3bf4db9461ba7d132`。
- 正式 APK SHA-256：`4DD30F621F811C4C9A5E43194A9BE630009E0AAEA1AFC9222C1722250DF311F2`；大小 6,769,135 字节。

## 交付边界

- 最终签名 APK、SHA-256、安装说明、审计结果、源码快照和本文件副本放在项目外层的 v2.12.6 交付目录。
- 签名材料不复制进仓库、不进入源码快照或交付记录。
- 不自动推送 GitHub；仅在用户之后明确要求时执行公开发布流程。
