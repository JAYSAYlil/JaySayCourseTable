# v3.4.15 课表界面收敛与交付验证

日期：2026-09-07。此版本将课表透明背景、紧凑顶栏、滑动响应、弹出动画、月视图信息和统一课程编辑器正式推进为 3.4.15 / versionCode 135。

本轮复核追加：移除自定义背景整图 blur，只保留课程方格透明层；月视图窄列不再隐藏课程名；日期/时间选择底部弹窗先执行 hide 再移除；必填星号改为红色；周次提示括号删除；学分、课程性质、线上开关和课程类别重新对齐；所有选择芯片统一尺寸。包名、数据 schemaVersion 4 和升级签名保持兼容，可覆盖升级 v3.4.14。

## 验证

- `:app:testDebugUnitTest --offline`：161 项 JVM 用例通过。
- `:app:lintRelease :app:assembleRelease --offline`：通过，0 个错误。
- APK 使用兼容升级密钥签名，v2/v3 验证通过。
- APK SHA-256：`C4956C5132F786080F888B8F2F2B4E82E1A22408F1889ED742BEB8793DAE5653`。
- Android UI 回归保留 60/61 通过记录；剩余 1 项为旧回归脚本在连续持久化数据下等待超时。
