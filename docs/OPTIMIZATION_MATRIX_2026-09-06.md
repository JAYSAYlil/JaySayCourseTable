# v3.4.13 优化与交付验证

日期：2026-09-06。起点：v3.4.12，Git `c326d9a`，开始时工作区干净。当前版本：3.4.13 / 133。

## 实现与边界

| 项目 | 实现 | 验证方式 |
|---|---|---|
| 保存竞态 | pendingViewModes 只负责展示；tables 只保留成功状态；请求序号淘汰旧请求；删除与恢复使旧索引请求失效 | ViewModeWriteGateTest；ViewModePersistenceTest 注入连续失败及周→日→月→日交错 |
| 月视图 | 根据 TextMeasurer 测得的实际像素高度分配日期、特殊安排、课程与农历；空间不足纵向滚动；“+N 项”准确计数 | MonthCellLayoutTest；MonthLargeTextTest；六行月与 2 倍字体截图 |
| 日视图 | 起止时间直接展示，教室用 14sp 半粗体并移到教师之前 | 长教室名视觉契约；浅深色截图 |
| 窄屏顶栏 | 今日摘要独立成行；离开今天的定位入口置于摘要行，课表名不再被第四个图标挤压 | 顶栏、月视图导航及详情返回 UI 回归 |
| Hero | 中断时保留实际矩形、四边速度及进度速度；重新测量后向新目标收束，读动画状态保持在绘制阶段 | 中断几何种子测试、详情旋转与滚动位置测试；真机高刷手感仍需确认 |
| 配色成本 | 缓存描边颜色及渐变 Brush，不重复执行对比度二分计算 | 真实配色矩阵测试与 Release 构建 |
| 服务状态 | 本机回执仅记录自动备份与小组件成功时间/最近失败；提醒描述区分权限检查和实际送达；支持修复权限、更换备份位置、主动刷新小组件 | 设置页面回归与截图；时间记录在操作成功/失败出口 |
| 基准模块 | Gradle 参数按需接入；独立 .benchmark 包；仅基准变体包含虚构数据准备入口 | 独立编译与模拟器连通性检查，实际性能结论须真机 |

数据格式仍为 schemaVersion 4；不新增正式应用权限。服务回执是本机诊断状态，不进入课程备份。未改动日/周 Pager 的导航来源与落定回写。

## 验证记录

- 基线 `:app:testDebugUnitTest --offline` 通过（baseline-3.4.13.log）。
- 首轮完整 UI 54 项中 1 项失败：日视图长教室名宽度出现省略。通过填满实际列宽修复，未放宽原有视觉断言。
- 最终代码完整回归：161 项 JVM 测试、60 项 Android UI 测试，均 0 失败、0 错误、0 跳过（final-verification-3.4.13.log，构建 3 分 32 秒）。
- 月导航经过普通/2 倍字号截图核对；新增计数溢出断言，统一文字测量与显示宽度，定向 6 项通过后再跑上述完整回归。此前失败与像素诊断日志另存，未弱化断言。
- Release Lint：0 错误、19 警告、1 提示。包括现有 SDK/依赖更新、资源未使用、排版和 KTX 建议；未以关闭 Lint 掩盖。
- Release 3.4.13 / 133 构建与 v2/v3 签名验证通过；2,324,386 字节。权限与 v3.4.12 相同，正式包没有 benchmark 准备入口或 profileable 标记，且不可调试。
- API 34 模拟器实际安装 v3.4.12 签名包后覆盖升级至 v3.4.13，启动成功；tables.json 与 preferences.json 哈希保持一致。此结果不代替用户真机及厂商后台验收。
- 最终六张虚构数据截图保存在本机交付记录的截图目录。

## 交付范围

本轮提供本地签名 APK、SHA-256、验证日志、截图和变更记录。尚未向远程推送或创建 GitHub Release。
真机未连接：不声称真实手机帧率、功耗或提升百分比；厂商后台提醒、真实桌面小组件及高刷新率动效属于真机验收范围。


## 基准实跑结果

2026-09-06：独立模块编译通过；API 34 模拟器 coldStart 与 detailRoundtrip 两项实跑通过（各 3 次预热、5 次测量），0 失败、0 跳过。显式忽略 EMULATOR 限制，仅用于连通性检查；日/周翻页和 2000 条课程场景已实现但本轮未实跑，真实前后性能对比仍待真机。
