# 版本变更记录

## v3.4.12（正式发布）— 2026-09-05

- 课程卡片文字对比度体系重写：标题/次级文字按实际渲染最不利背景动态派生（标题 5.5:1、次级 4.5:1，旧实现次级仅约 2.2:1）；深色渐变高光亮度封顶，自定义背景遮罩关闭时文字仍稳定达标；增强对比度开关现在真实改善课程文字与描边，课程色相辨识保留。
- 跨午夜日期状态修复：应用长时间停留后跨天，今日高亮、周次徽标、今日摘要自动更新（回前台 + 系统日期/时区广播触发），不改变用户浏览位置，无新增轮询。
- Hero 详情转场中断续接：正向飞行中途返回、快速连续进出、旋转重建，飞行矩形从当前在途位置续接，不再先跳端点再重新起飞。
- 周视图信息层级：字号按实际列宽与系统字体缩放分档（下限 9sp/8sp，废除 7sp），放不下先减信息再缩字，截断改用省略号；新增“周视图精简课程卡片”设置（默认关闭）。
- 月视图：日期数字加大，课程行文字改按中性单元格背景取色；矮行按“先隐农历、再减课程名”降级为“N 门课”，停课/节假日/补课状态任何档位保留。
- 顶栏操作重排：紧凑宽度导入收进更多菜单（空课表仍保留明显导入入口）；浏览非今日日期时“回到今天”直达；搜索框展开即持焦。
- 视觉层级收敛：普通课程阴影降低，正在上的课以更重阴影 + 品牌描边突出；卡片圆角归入形状令牌。
- 性能：课程配色映射一次性构建（月视图 O(n²)→O(n)）；视图模式切换即时生效、后台持久化，且不再产生无意义的课程历史快照；新增 Macrobenchmark 测量设施（真机测量待做）。
- 维护性：主屏静态展示区块提取为独立文件（行为不变）；新增对比度真实渲染矩阵、跨午夜时钟、Hero 续接种子等 24 项 JVM 回归测试。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.12（versionCode 132）。
- 交付包已完成签名、zipalign 与证书校验，可从 GitHub Releases 获取。

## v3.4.11（正式发布）— 2026-09-02

- 深色模式课程卡片渐变加强：由 +14%/-10% 的收敛渐变改为三段式光影（顶部高光 +26%、中段回落基色、底部压暗 -22%），光源感明显增强且不稀释色相；浅色模式维持原有平整卡片。
- 课表页翻周进度条重设计：2dp 单色细条替换为 4dp 圆角胶囊轨道，填充为品牌双色（primary → tertiary）渐变，当前周位置带一枚柔光圆点头部，深浅色主题各自取材。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.11（versionCode 131）。
- 最终优化：Release 包启用 R8、资源收缩与中文资源裁剪，交付 APK 保持约 2.30 MB；日视图不再预加载视口外的完整课表页，顶部星期跟随 Pager 当前页即时更新，并减少单日网格的无效分组重组。
- 交付包已完成签名、zipalign 与用户真机验证，可从 GitHub Releases 获取。

## v3.4.10（内部测试版）— 2026-09-01

- 根治全部视图的翻页乱跳：日视图的周次/星期回写副作用此前常驻所有视图，屏幕重回组合（打开课程详情返回、切换视图）时会把共享周次拉回日视图旧值，周视图随即被同步动画拽到错误周。现在回写只在日视图可见时激活，且日/周两个翻页器都以"效果启动时的落定页"为基线，只有真实翻页才回写——滑到哪里就是哪里。
- 月视图不再在每次进入时重置月份：切走再切回、进出详情都保持用户上次所在月份；仅学期起点或总周数真正变化（换表、改学期设置）时按当前周重建。
- 日视图翻页改为共享同一纵向滚动位置：翻到相邻一天停在上一天的阅读位置，与周视图一致；离开主界面再返回同样恢复。
- 开学日期选择器保存用户选择的原日期，不再在保存侧改写为周一（周次计算在读取侧统一归一，不受影响），重新打开时忠实还原所选那一天。
- 开学日期选择器新增年份快速选择条：横向点选年份直接跨年跳转，范围以所选日期为中心前后各 20 年，并跟随可见月份自动居中。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.10（versionCode 130）。内部测试版，不发布 GitHub。

## v3.4.9（内部测试版）— 2026-09-01

- 彻底重做日视图：Pager 页码成为日期唯一事实来源，顶部星期、日期表头、周次和课程页从同一状态即时派生，删除旧的预览日期、落定日期、待处理令牌之间的双向回写。
- 日视图慢拖与快速甩动时，顶部星期会在目标页确定时立即响应；单次手势最多推进一天，定位动画可被新手势或新定位请求直接打断并从当前画面续接。
- 恢复并实测“定位到今天”：跨周、快速翻页后和学期边界场景统一走日视图控制器，学期外日期会钳制到最近边界，不再被旧页面落定结果覆盖。
- 浅色主题的全部 Material 表面角色统一为纯白；三点菜单和视图菜单显式使用白色容器、细描边与轻阴影，不再出现紫粉色默认底。
- 开学日期改为应用自绘月历底部面板；节次时间改为可打断、自动吸附的 24 小时双滚轮面板，取消 Android 默认日期/时钟弹窗观感。
- 设置页搜索框首屏顶部留白调整为 20dp，改善其与顶栏分割线、下方分区之间的视觉平衡。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.9（versionCode 129）。内部测试版，不发布 GitHub。

## v3.4.8（内部测试版）— 2026-09-01

- 日视图翻页时改为由 Pager 当前页即时驱动顶部星期、日期与周次显示；落定后再回写正式状态，减少顶部响应延迟。
- 恢复并加固日视图“定位到今天”：外部定位请求带有独立令牌，正在滑动/动画期间也不会被中途落定页覆盖。
- 浅色主题页面背景统一为纯白，保留课程卡片与控件的层次对比，避免旧粉色/非统一底色残留。
- 设置页内容增加对称的顶部留白，搜索框与后续设置项的纵向间隔更均衡。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.8（versionCode 128）。内部测试版，不发布 GitHub。

## v3.4.7（内部测试版）— 2026-09-01

- 修复日视图与月视图快速滑动可能跨越多个页面的问题：Pager 的单次手势最多只推进一页，并提高衰减摩擦，避免高速 fling 跳日/跳月。
- 修复日期外部同步在用户拖动期间被丢弃的问题：目标日期会等待 Pager 空闲后再应用，不会与当前手势抢状态。
- 修复日视图翻页后周次与星期焦点偶发滞后的问题：统一由 settledPage 回写显示日期、周次和星期焦点，并增加相邻日期双向翻页回归测试。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.7（versionCode 127）。内部测试版，不发布 GitHub。

## v3.4.6（内部测试版）— 2026-09-01

- 修复日程列表与停课/特殊安排页面内容与顶栏重叠的问题：v3.4.0 引入的"内容滚入半透明顶栏之下"布局在部分页面漏配了顶部占位，首行内容被顶栏遮挡。本轮整体回退该机制，所有页面内容恢复在顶栏下方布局，此类冲突从结构上根除。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.6（versionCode 126）。内部测试版，不发布 GitHub。

## v3.4.5（内部测试版）— 2026-09-01

- 修复日视图定位与初始定位的跨周错位：此前日视图的显示日期由（周数, 星期数）两个独立状态拼回，"定位到今天"在显示周与今日不同周时会错落到显示周的同星期数那天（用户报告：开学 9/7、今天 9/1 时被定位到 9/8）。
- 重构为单一日期源：日视图翻页器、星期条、日期表头全部由同一个"显示日期"状态派生，所有跳转源（定位今天/星期条/表头/月视图点日期）直接写该状态，无并行通道无竞态。
- 今天在学期外（开学前/学期结束后）时，初始定位与"定位到今天"统一钳制到最近的学期边界（开学前 → 开学第一天）。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.5（versionCode 125）。内部测试版，不发布 GitHub。

## v3.4.4（内部测试版）— 2026-09-01

- 修复日视图“定位到今天”按星期数而非日期定位的问题：此前日视图跳转目标由（周数, 星期数）两个独立状态拼回日期，两者异步更新时会把中间态拼成错误的日期（表现为“定位到今天”跳到了显示周的同星期数那一天，且滚动守卫会丢弃随后到达的正确跳转）。现改为日期令牌制：定位今天、点星期条、月视图点日期都直接下发目标日期，翻页器精确跳页后回写周次与星期。
- 实测验证：从第 1 周的 8/26 定位今天，正确跳到第 2 周的 9/1（今日真实日期）。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.4（versionCode 124）。内部测试版，不发布 GitHub。

## v3.4.3（内部测试版）— 2026-09-01

- 修复日视图滚动位置不保存：单日网格的滚动状态改为可保存（此前切到其他页面再返回会回到顶部，周视图一直正常）。
- 课程详情转场细腻感回归：详情页进出改为专属的"淡入 + 从头部原点轻微缩放"对称弹簧转场，与 Hero 飞行矩形（同族临界阻尼弹簧）同步进行，形成完整的卡片飞入展开观感（此前为生硬的交叉淡化或水平滑动，与垂直飞行割裂）。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.3（versionCode 124）。内部测试版，不发布 GitHub。

## v3.4.2（内部测试版）— 2026-09-01

- **彻底重做 Hero 转场实现**：移除 SharedTransitionLayout/sharedBounds（其 bounds 实时跟踪与 HorizontalPager/滚动容器冲突，导致转场不触发、课程卡越界绘制、卡片消失，两次修补无效），改为手动 overlay 飞行：
  - 课程卡在布局回调中把「可见卡片」的 root bounds 与底色快照进注册表（视口外的 Pager 相邻周页卡片不覆盖）；
  - 点击瞬间快照起点，详情头部测量后获得终点，overlay 以临界阻尼弹簧画一次性飞行的圆角矩形（bounds/圆角/底色透明度同时插值，起终点各 20% 行程淡入淡出与真实卡片/头部无缝交接）；
  - 动画期间不跟踪任何真实元素——滚动、翻页、页面进出与飞行完全解耦，从机制上消除此前全部症状。
- 进出详情页的页面级转场改为短淡入淡出（原先进入详情是右侧滑入），把主要运动交给 Hero 飞行。
- 反向飞行在离开详情时以注册表当前卡片位置为终点（回退到快照值），返回后卡片无消失、无越界。
- JVM 138 项、API 34 UI 40 项、Release Lint 0 错误全部通过；模拟器实测滚动后点击、翻周后点击、快速往返三场景均正常。
- 数据格式向后兼容。版本 3.4.2（versionCode 123）。内部测试版。

## v3.4.1（内部测试版）— 2026-09-01

- 修复 v3.4.0 的两处性能与布局回退：
  - 按压反馈由 Modifier.composed 实现（每实例创建独立子组合，月视图 40+ 日期格与全部课程卡挂载后造成明显掉帧）改为直接组合的修饰符工厂，缩放值经 graphicsLayer 读取（只触发重绘，不触发重组）。
  - 共享元素改为 origin 令牌制：只有被点击的课程卡注册 sharedBounds。此前同 series 的卡片在相邻周页同时组合时产生重复 key，导致滑动过程中课程方格布局溢出。
- 数据格式向后兼容，可直接覆盖升级。版本 3.4.1（versionCode 122）。内部测试版。

## v3.4.0（内部测试版）— 2026-08-31

- **共享元素转场**：点击课程卡片，卡片以 container transform 过渡为课程详情页头部（Compose SharedTransitionLayout），返回时反向收回。
- **图标系统**：全部图标切换为 Material 圆角字形（Icons.Rounded），观感向 SF Symbols 的圆润气质靠拢，零安装包体积成本。
- **半透明材质顶栏**：设置/日程/详情/历史/学期安排/导入确认各页顶栏改为 90% 半透明表面，列表内容滚动时从顶栏之下穿过（Apple materials 的滚入其下层级）。
- **课程详情下拉关闭**：按住详情页头部卡片下滑，位移带阻尼跟手；快速下滑或位移过半即关闭，否则临界阻尼弹回。
- **预测性返回**：启用系统预测性返回回调（Android 14+ 手势预览），应用内返回逻辑不变。
- 版本 3.4.0（versionCode 121）。内部测试版，不发布 GitHub。

## v3.3.2 — 2026-08-31

- 修复月视图分页偶发自行左右滑动的问题：收敛月份状态同步链，避免分页落定与外部锚点回写互相触发动画。
- 月视图页面使用稳定月份 key，并关闭额外预加载，减少重组时旧页面状态复用造成的视觉跳动。
- 保留用户主动翻页的触觉反馈，首次进入月视图不再误触震动。
- 版本 3.3.2（versionCode 121）。已签名并发布 GitHub Release。

## v3.3.0（内部测试版）— 2026-08-31

- 依据 Apple 设计准则（emilkowalski apple-design + HIG 设计审查库）整体重制动效与排版；主题绿色保持不变。
- 动效体系全面转向 Apple 临界阻尼弹簧：交互与页面级弹簧无过冲、可随时中断续接；页面进出改为同路径同弹簧（空间一致性）；日历提示条展开/收起改用带惯性的弹簧。
- 排版引入字号专属字距：大标题负字距、行距随字号收紧，小标签轻微正字距；课表大标题应用新字距。
- 按压反馈全覆盖：课程卡片、月历日期格、日视图星期条按下瞬间缩放（不等抬手）。
- 触觉反馈：日/周/月翻页落定瞬间轻点一下（CLOCK_TICK），只在导航提交节点。
- 周进度条进度平滑动画推进。
- 版本 3.3.0（versionCode 119）。内部测试版，不发布 GitHub。

## v3.2.6 — 2026-08-31

- 修复日视图滑动翻页后，顶部星期导航与周次胶囊不跟随刷新的问题：滑动回写与外部状态回正之间存在竞态，现在以“双方确认的日期令牌”解耦两侧同步，滑动、定位今天、表头点击、月视图跳天互不干扰。
- 数据格式向后兼容，可直接覆盖升级。版本 3.2.6（versionCode 118）。

## v3.2.5 — 2026-08-31

- 日视图星期导航增加“今日”标识：本周内今天的天数以加粗字重与品牌色圆点标注，与周视图表头的今日高亮语义一致。
- 日视图翻页范围限制在学期日期内：学期第一天到第 N 周最后一天之外的日期不可再翻到，修复“没有内容仍能继续翻页”的问题。
- 数据格式向后兼容，可直接覆盖升级。版本 3.2.5（versionCode 117）。

## v3.2.4 — 2026-08-31

- 日视图支持左右滑动按天切换；一周滑完继续滑即自动翻到下一周（反向同理）。
- 月视图改为按月翻页：滑动跨度从周改为月，头部胶囊显示“某年某月”与本月课程数，跨度覆盖开学月份到学期最后一个月；左右箭头与滑动联动，点击月内某天仍直接跳到该天的单日视图。
- 月视图隐藏周进度条（周进度对月跨度无意义）；月历翻页时月份与课程即时刷新。
- 月视图头部课程统计改为“本月共 N 节课”（按实际上课节次计数，含同课程多班次），替代原先的去重课程名数。
- 返回操作分流：从月视图点具体日期进入日视图后，返回手势切回月视图（回到当前查看日期所在月份）；从视图菜单或表头点击进入日视图时，返回行为保持原样。
- 新增日视图/月视图分页导航回归测试；JVM 136 项、API 34 UI 37 项、Release Lint 0 错误全部通过。
- 数据格式向后兼容，可直接覆盖升级。版本 3.2.4（versionCode 116）。

## v3.2.3 — 2026-08-31

- 移除"跟随系统取色"开关与相关偏好字段：主题固定使用内置青绿配色（浅/深双模式不变），设置页更简洁；旧数据中的该字段会被安全忽略。
- 移除周视图/五天视图/单日视图底部的 48dp 收尾空隙：节次时间线重设计后时间标签不再需要额外滚动余量，课程网格直接铺满到底。
- 数据格式向后兼容，可直接覆盖升级。
- 版本升级为 3.2.3（versionCode 115）。

## v3.2.2 — 2026-08-31

- 课程节次时间线重设计：时间列与课程网格的衔接、当前时段指示与配色重排，扫读更直观。
- 版本升级为 3.2.2（versionCode 114）。

## v3.2.1 — 2026-08-31

- 节次时间显示优化：时间列由"圆形序号 + 上下堆叠时间"改为整块卡片式呈现（节次序号 + 起止时间段），更易扫读；课表页头部配合微调。
- 版本升级为 3.2.1（versionCode 113）。

## v3.2.0 — 2026-08-31

- 导入草稿新增串行写入与 revision 闸门，修复暂存/确认/取消快速连续操作时旧草稿可能晚于清除任务落盘的问题。
- Excel 轻量解析器增强兼容性：优先选择第一个可见工作表，支持合并单元格表头和 Excel 1904 日期系统；继续保留压缩解压上限，不引入 POI-OOXML。
- 目标工作表改用 SAX 流式解析，避免大表格构造完整 DOM；增加行数与单行单元格上限，降低低端设备解析峰值。
- Excel 错误提示按文件权限、压缩包、XML 结构、文件截断和大小限制分类，减少底层异常直接暴露给用户。
- 课表持久化对完全相同状态直接跳过文件同步，历史快照增加 1 秒编辑窗口合并（恢复操作强制保留回滚点）；导入确认页缓存选中课程集合，降低重复回调和重组成本。
- 手动检查更新增加 HTTP 状态判断与 256KB 响应上限，避免异常服务响应无限读入内存。
- 设置页更新检查条目独立拆分；新增 150% 字体导入确认、跨日/时区小组件和快照合并回归测试；十项优化矩阵见 `docs/OPTIMIZATION_MATRIX.md`。
- 数据格式未变化（schemaVersion 4），可直接覆盖升级。版本升级为 3.2.0（versionCode 112）。

## v3.1.2 — 2026-08-31

- 修复瘦身版 `.xlsx` 在 Android 真机上必然导入失败的问题：轻量解析器不再调用 Android XML 实现不支持的 XInclude 配置；保留 zip + DOM 的小体积方案，不恢复体积庞大的 POI-OOXML 依赖。
- 新增 Android 端真实解析回归：直接用系统 XML 实现读取安装包内的 `.xlsx` 模板，防止 JVM 测试通过、手机端却失败的环境分叉再次出现。
- 保留 `.xls` 的按需 POI-core 支持与 `.xlsx` 的轻量解析路径；三份真实学校 `.xlsx`、内置模板、合成 `.xlsx`/`.xls` 均纳入回归验证。
- 按要求彻底移除整周网格桌面小组件，包括 Provider、Manifest 注册、刷新链、布局、预览资源与专属测试；继续保留稳定的“今日/明日课程”响应式小组件。
- 数据格式未变化（schemaVersion 4），可直接覆盖升级。版本升级为 3.1.2（versionCode 111）。

## v3.1.1 — 2026-08-31

- 完善**整周网格桌面小组件**：改用兼容各类桌面启动器的位图网格渲染，修复原实现依赖嵌套权重布局、实际添加后只显示表头或课程区域空白的问题。
- 小组件会根据当前尺寸动态计算 7 天列宽和节次行高；课程色块、课程名、地点、跨节次高度与今天高亮均在同一网格内稳定呈现，紧凑尺寸下自动收敛信息密度。
- 同一单元格存在多门课程时显示“+N”，整张小组件保持点击进入应用；深浅色模式、课表编辑刷新和系统尺寸变化继续沿用原刷新链。
- 修正小组件选择器预览图，使预览与实际的“星期表头 + 整周课程网格”结构一致。
- 新增网格尺寸与课程区间几何回归测试；JVM 单元测试、Release 构建与 Lint 均通过，并在 API 34 模拟器完成真实桌面添加与刷新验证。
- 数据格式未变化（schemaVersion 4），可直接覆盖升级 v3.1.0 及更早版本。版本升级为 3.1.1（versionCode 110）。

## v3.1.0 — 2026-08-30

- 新增**月视图**：视图切换新增“月”，以当前周所在月份展示整月课程（与周视图同色、同口径处理单双周/停课/调课），今天高亮、停课日置灰，点击任一天直接跳到该天的单日视图。
- 新增**整周网格桌面小组件**（4×2 起步、可拉伸）：星期标签行 + 整周课程网格，今天列品牌色高亮，配色与 App 内卡片一致（浅/深色自动切换）；整个小组件可点击进入应用；与“今/明”小组件共用刷新链，课表编辑即时跟随。
- **安装包大幅瘦身**：Excel 导入从 Apache POI 替换为手写的最小 xlsx 解析器（zip + DOM），保留表头别名识别、行级错误报告、20MB 护栏等全部行为，新增公式缓存值与日期单元格支持；APK 从 6.8MB 降至 2.3MB（-66%）；.xlsx 走自研轻量解析器，旧版 .xls 由按需引入的 POI-core（仅 OLE2 魔数时启用）读取，学校教务系统导出的 .xls 可直接导入。
- 设置页重新分组为四张卡片：通用（主题/取色/背景/无障碍）、上课提醒、学期设置、数据与版本（自动备份/检查更新/备份恢复/诊断）。
- 周视图/五天视图/单日视图删除底部收尾空隙，滚动到底与月视图一致。
- 修复整周网格小组件不跟随课表编辑刷新的问题（框架刷新 action 未加入白名单）。- 新增“检查更新”（设置 → 通用）：手动触发访问 GitHub Releases 查询新版本，可一键打开发布页；应用仍无任何常驻联网行为（新增 INTERNET 权限仅供该手动操作使用）。
- 新增“启动时自动备份”（默认关闭）：选择位置后每次启动把全部课表静默覆盖写入该文件，防数据损坏。
- 自定义背景读取端降采样，低端机内存占用显著下降。
- 新增 GitHub Actions CI：每次推送自动跑 JVM 测试 + Lint + Release 构建。
- Android 13+ 主题图标（monochrome）此前已内置，本版无变化。
- 数据格式新增两个可选偏好字段（自动备份开关/位置），旧数据可无缝升级，仍为 schemaVersion 4 兼容。
- 版本升级为 3.1.0（versionCode 109），可直接覆盖升级 v3.0.0 及更早版本。

## v3.0.0 — 2026-08-30

- 全新视觉体系（UI/UX 3.0 大改版）：引入统一设计令牌（形状 12/16/18/20/28dp 圆角体系、间距刻度、大标题式排版刻度），全部界面按新令牌重塑，整体观感向系统级应用的克制风格看齐。
- 课程卡片与调色板重做：15 色低饱和莫兰迪系浅色 + 同色相暗调深色双套调色板；卡片文字颜色改为由底色按亮度自动派生（标题/次级双色），自定义颜色同样适用；正在上的课描边平滑过渡到品牌色并加重阴影，形成柔和高亮。
- 主课表页：周次切换重构为 HorizontalPager，滑动全程跟手、带边缘回弹，翻页结束后回写周次；纵向滚动位置在翻页间继续保留；周导航、视图切换、日视图选择条与校历提示条按新令牌重绘；今日安排摘要改为底部抽屉展示。
- 动效体系：新增 Motion 令牌（交互弹簧/页面弹簧/强调缓动），页面切换改用弹簧曲线；停课/特殊安排提示条在切入相关周次时展开淡入、离开时收起；设置页条目按压反馈缩放；今日摘要条改为底部抽屉滑入。原“减少动态效果”开关随本版移除（动效本身轻量，无需全局降级入口），低端机可直接受益于更轻的动画负载。
- 设置页重排：改为分组卡片式布局并新增设置搜索；新增“跟随系统取色”开关（Android 12+ 壁纸动态取色，关闭时保持内置青绿主题）。
- 课程编辑弹窗底部按钮统一尺寸：删除（本周/全部）、取消、保存三键等分宽度、同高 44dp，“删除全部”四字在窄屏弹窗内也完整显示。
- 深色模式整体重做：切换为中性深灰底色体系，浅色端改用微灰白背景 + 纯白卡片拉开层次；课程卡片、设置页、弹窗、桌面小组件（values-night）全部对齐新配色。
- 桌面小组件配色与圆角对齐 App 内卡片体系（浅色/深色双套）。
- 数据格式未变化（schemaVersion 4），功能逻辑零改动，可直接覆盖升级 v2.20.1 及更早版本。
- 版本升级为 3.0.0（versionCode 108）。

## v2.20.1 — 2026-08-27

- 桌面小组件新增课表名称显示：与日期同行显示为“8月27日 · 课表名”，不新增行高、不挤压课程列表；名称超长随日期一起单行省略，无课表或名称为空时只显示日期；无障碍描述同步包含课表名。
- 新增课表名标签单元测试；JVM 123 项、API 34 UI 30 项、Release Lint 0 错误全部通过；模拟器实测放置小组件确认同行显示正常。
- 版本升级为 2.20.1（versionCode 107），可直接覆盖升级 v2.19.0 及更早版本。

## v2.19.0 — 2026-08-27

- 性能修复：30 秒分钟的重组范围真正收敛到“今日摘要条”。此前分钟状态在课表屏幕顶层被读取，导致整屏（周导航、日期表头、日程条）每 30 秒重组一次；现将分钟订阅下沉到独立的今日摘要子组件，课表网格与课程卡片不受影响。
- 旋转/进程回收不再丢输入：课程新增/编辑弹窗的全部约 20 个表单字段整体持久化（单一可序列化表单状态），同时修复旧版旋转后随机系列 ID 重新生成导致系列关联漂移的隐患；详情页删除确认、加密导出密码框等弹窗开关也按稳定标识恢复。
- 稳定性：上课提醒、提醒恢复、桌面小组件三个广播接收器的协程体补齐兜底捕获，未预期异常不再击穿默认异常处理器崩溃进程；Excel 导入增加 20 MB 文件大小护栏（POI 整本载入内存前的第一道拦截，与备份导入标准对齐）。
- 提醒调度收窄：切主题、换背景、开遮罩等纯外观偏好变化不再触发“取消并重建全部闹钟 + 两次同步落盘”，只有课程数据或提醒相关偏好（总开关/提前分钟）变化才重新排程；提醒暂停状态、权限检查和小组件存在性检查的 SharedPreferences 读取改为按需缓存。
- 一致性收敛：学期周次计算统一为 TimeUtils 的唯一公式（此前 5 处副本 3 种语义，越界周次在不同页面可能不一致）；课表保存路径删除三重冗余编码（重复 normalize 与 diff 前的二次全量解析）。
- 细节完善：导入草稿写盘失败留诊断日志；“上午/下午/晚上”分区、“第 X 周”、日期格式等漏网文案补入字符串资源；课程颜色色块与星期选择补无障碍语义（TalkBack 可播报选中状态）；编辑目标/删除确认以 seriesKey 持久化并在目标失效时自动收起。
- 数据格式未变化（schemaVersion 4），可直接覆盖升级 v2.18.2 及更早版本。JVM 单元测试 122 项、API 34 UI 测试 30 项、Release Lint 0 错误全部通过；模拟器实测冷启动 ×3 无崩溃、新增课程弹窗旋转后内容保留。

## v2.18.2 — 2026-08-26

- “数据备份与恢复”板块新增“下载 Excel 导入模板”：将内置空白课表模板（含表头示例）保存到用户选择的位置，方便自行制作 Excel 导入文件；模板随安装包内置，无需联网。
- 版本升级为 2.18.2（versionCode 104），可直接覆盖升级 v2.18.1 及更早版本。

## v2.18.1 — 2026-08-26

- 课表页“本周特殊安排/停课周”提示条增加直达入口，点击即可进入“学期安排”；从该入口返回时准确回到课表，从设置页进入时仍返回设置页。
- “具体日期安排”的添加按钮改为与“周标签”一致的文字按钮；已有日期安排整行可点击编辑，日期、类型、课程和备注均会预填，保存时保留原 ID 并原位更新，不产生重复记录。
- 自定义背景增加“可读遮罩”开关：默认开启以保持旧版可读性，关闭后移除课表全屏明暗遮罩以突出背景；设置持久化并兼容旧设置/旧备份。
- 新增日期安排编辑、校历提示条点击、遮罩开关和旧备份兼容回归；JVM 122 项、API 34 UI 29 项全部通过。
- 版本升级为 2.18.1（versionCode 103），可直接覆盖升级 v2.18.0 及更早版本。

## v2.18.0 — 2026-08-26

- 重做“学期安排”设置页：停课周、周标签和具体日期安排集中在统一卡片式界面，20 周及更长学期均可完整滚动选择，修改即时保存并显示汇总。
- 停课周不再进入独立的不可滑动分支；即使本周停课或搜索无结果，仍可在课表主体正常左右滑动切周，停课周内存在补课时继续显示补课课程。
- 课表周次胶囊、校历提示条和日期表头会显示周标签、整日停课、单课取消及补课数量；桌面小组件同步显示周标签/周次和日期调整原因，避免只看到课程变化却不知道原因。
- 新增统一的 `AcademicCalendarStatusResolver`，课表和小组件共用同一校历状态解释，并补充最远周次选择、即时保存、停课周滑动与小组件状态文案回归测试。
- 新增视觉契约回归：覆盖七天/五天/单日、深浅色、自定义背景和 320×640 紧凑屏，自动检查主控件、长课程名、教师、教室的可见性与排版溢出，并检测深色模式整屏白光或空白退化。
- 修复单日视图中部分中长中文课程名在字形末端发生亚像素裁切的问题：课程名、教师和教室文本使用完整卡片宽度排版，保留原字号和信息密度。
- 桌面小组件升级为新旧系统双实现：Android 12+ 直接使用 `RemoteCollectionItems`，Android 8–11 保留 `RemoteViewsService`；两条路径共用课程条目布局与稳定 ID，减少刷新分叉和新系统弃用接口依赖。
- 提醒恢复接收器增加系统事件白名单，只响应重启、应用升级、时间和时区变化；移除厂商检测中的系统私有 API 反射，保留明确的制造商识别与安全回退。
- 自定义背景的旧设备方向校正改用 AndroidX EXIF 解析器，避免依赖系统旧实现的已知解析缺陷。
- 动态快捷方式上报匿名使用次数，帮助桌面启动器合理排序“查看今天/新增课程”，不上报课程数据。
- 结构优化：课表网格/课程卡片拆分到 `ScheduleGrid.kt`，小组件日期解析拆分到 `WidgetSchedule.kt`，统一返回规则拆分到 `AppNavigation.kt`；界面尺寸、颜色、透明度和动画逻辑保持不变。
- 新增 JDK 17 构建预检脚本与版本/SDK/Gradle 文档同步审计；脚本仅约束本次 Gradle 子进程，不修改系统 Java、网络或代理配置。
- 数据格式未变化：tables/preferences 继续使用 schemaVersion 4，可直接覆盖升级 v2.17.1 及更早正式版本。
- 版本升级为 2.18.0（versionCode 102）。

## v2.17.1 — 2026-08-21

- 重做周次切换滚动位置保持：改为切周时直接继承当前纵向位置（上一版按“每周围绕独立记忆”实现，导致首次切周回顶、切回乱跳）。
- 实现：纵向滚动状态提升到周次切换动画之外、独立于周次，切换/返回详情/Activity 重建均保持同一位置；移除上版的按周位置记忆与恢复逻辑。
- 版本升级为 2.17.1（versionCode 101），可直接覆盖升级 2.17.0 及更早正式版本。

## v2.17.0 — 2026-08-21

- 周次切换保持纵向滚动位置：从本周切换到另一周时，会停在该周上次浏览的相同纵向位置，切回来也保持原位置。
- 提醒自启动引导文案由“自启动”改为“后台活动”（实测开启后台活动才能在杀掉后台后正常触发通知），涉及设置页提示、确认框与引导弹窗。
- 版本升级为 2.17.0（versionCode 100），可直接覆盖升级 2.16.x 及更早正式版本。

## v2.16.2 — 2026-08-21

- 删除设置页“提醒功能正常，将按节次时间提前通知”提示：若用户未开启系统自启动，即使打开提醒开关也无法正常提醒，原提示属于误导。
- 版本升级为 2.16.2（versionCode 99），可直接覆盖升级 2.16.1 及更早正式版本。

## v2.16.1 — 2026-08-21

- 移除电池优化提示与“允许后台运行”按钮（白名单方案下不再需要，交由自启动统一处理）。
- 自启动入口整合：提醒开启或桌面存在小组件时，在设置页显示“打开自启动设置”入口（作为开启提醒时点“稍后”后的补救通道）；未开启提醒且无小组件时不显示。
- 文案更新：明确“几乎所有安卓手机都需开启自启动，才能在杀掉后台后准时提醒、小组件正常更新”。
- 小组件跨天/上课下课边界刷新改用精确闹钟（有精确闹钟能力时），天数与状态更新更准时；自启动限制仍建议在系统设置允许。
- 版本升级为 2.16.1（versionCode 98），可直接覆盖升级 2.16.0 及更早正式版本。

## v2.16.0 — 2026-08-21

- 移除前台保活服务与相关机制（保活通知、服务声明、前台服务权限）——厂商 ROM 上“划掉后台即冻结”无法被应用绕过，改为尊重系统白名单、把选择权交给用户。
- 上课提醒改为“默认关闭 + 开启确认”：打开开关时先弹窗说明后台运行可能影响续航，确认后才开启。
- 开启后引导跳转系统自启动设置：新增 AutostartHelper，按厂商（小米/华为/OPPO/vivo/三星）尝试跳转自启动管理页，失败回退应用详情设置页；设置页保留“打开自启动设置”入口。
- 删除用户用不到的测试功能：测试通知按钮、触发诊断记录、相关诊断代码。
- 清理废弃代码：ReminderKeepAliveService、ReminderDiagnostics 及其全部接线；过期暂停清理保留并移到应用打开重排时执行。
- 提醒调度仍使用 setAlarmClock 用户可见闹钟，通知权限/精确闹钟/通知渠道/电池优化引导保留。
- 提醒 E2E 测试精简为 3 项；JVM 114 项、API 34 UI 测试 22 项全部通过。
- 版本升级为 2.16.0（versionCode 97），可直接覆盖升级 2.15.x 及更早正式版本。

## v2.15.1 — 2026-08-21

- 按用户反馈移除“暂停后推送恢复提醒通知”：点“今天不再提醒/本周暂停”后不再多发一条通知，避免通知过多打扰。
- 暂停后的恢复入口只保留设置页：设置 → 上课提醒 → “恢复提醒”按钮（暂停状态有效时显示）。
- 暂停/恢复动作仍记录到“最近触发记录”，便于排障。
- 版本升级为 2.15.1（versionCode 96），可直接覆盖升级 2.15.0 及更早正式版本。

## v2.15.0 — 2026-08-21

- 重做提醒后台机制：新增轻量前台保活服务，应用未打开/被划掉后台时进程保持存活，闹钟触发即时执行。
  - 无独立进程（与主应用同进程，内存零额外开销）、无 WakeLock、无联网、仅 60 秒一次内存级心跳；
  - 常驻通知使用静默渠道（不响铃不振动），Android 13+ 拒绝通知权限时服务照常运行；
  - 仅在“提醒已开启”或“桌面存在小组件”时运行，其余时间自动退出；
  - 提醒关闭时显式停止，提醒开启/闹钟触发/小组件添加时自动启动，被杀后自动重启（START_STICKY）。
- 桌面小组件接入保活：服务心跳按“当前/下一节课”状态指纹变化才刷新小组件，配合小组件原有的上课/下课边界闹钟，不打开软件也能更新课程状态。
- “今天不再提醒/本周暂停”恢复入口全程可见：点击暂停后立即推送一条“已暂停，点此恢复”通知（含恢复按钮），设置页保留恢复入口；服务心跳自动清理已过期的暂停状态。
- 设置页新增厂商自启动提示与“打开应用系统设置”引导（小米/华为等需允许自启动才能离线提醒）。
- 调度器新增诊断记录：每次重排都记录“已重排 N 条/无未来提醒”，排障可直接看到调度结果。
- 提醒 E2E 测试增至 5 项（新增保活服务启停、暂停→恢复全链路）；模拟器实测杀进程后服务自愈、闹钟准点触发通知、诊断完整。
- 版本升级为 2.15.0（versionCode 95），可直接覆盖升级 2.14.x 及更早正式版本。

## v2.14.1 — 2026-08-21

- 针对“划掉后台/锁屏后提醒仍不触发”的修复：调度改用 setAlarmClock 用户可见闹钟（系统状态栏闹钟图标，Doze、应用待机、省电模式及多数厂商后台清理下依然准点触发，且不需要精确闹钟权限），精确闹钟权限可用时保留原精确调度、不可用时降级近似调度。
- 设置页提醒区新增“发送测试通知”一键验证按钮，立即验证“应用 → 系统通知”链路是否通畅。
- 新增提醒触发诊断记录：每次闹钟触发都会记录时间与结果（通知已发送/权限拦截/课程已变更/已暂停等），设置页可查看最近 3 条，排障不再靠猜。
- 设置页新增电池优化检查：应用受电池优化限制时显示警告并可一键跳转系统设置允许后台运行。
- 通知权限检查补全为 lint 可验证的显式检查；提醒 E2E 测试增至 3 项（新增测试通知链路验证）。
- 模拟器真实时间实测：setAlarmClock 闹钟准点触发、通知弹出、诊断记录正确写入。
- 版本升级为 2.14.1（versionCode 94），可直接覆盖升级 2.14.0 及更早正式版本。

## v2.14.0 — 2026-08-21

- 修复上课提醒"到时间不通知"问题：新增 USE_EXACT_ALARM 权限（Android 13+ 自动授予、不可撤销），根治 Android 14+ 对 SCHEDULE_EXACT_ALARM 默认拒绝导致的精确闹钟降级与提醒不准时。
- 设置页提醒区新增状态检查与一键修复：通知权限未开启、精确闹钟被系统拒绝、“上课提醒”通知渠道被关闭时分别显示警告与引导按钮；一切正常时显示确认提示。
- 从系统设置返回后自动刷新提醒状态提示。
- 体积优化：R8 排除 POI 中课表用不到的 PowerPoint/Word/Visio/邮件与数字签名模块，并移除幻灯片渲染等非 Excel 资源，APK 由 6,984,274 字节降至约 6,484,000 字节（-7.2%）。
- 语言资源裁剪迁移到 androidResources.localeFilters（resourceConfigurations 已弃用）。
- 新增提醒端到端回归测试（精确闹钟可用性、闹钟广播→通知全链路）与设置页提醒区/权限警告 UI 测试；JVM 测试 114 项、API 34 UI 测试 21 项全部通过。
- 模拟器真实时间实测：闹钟按提前分钟数准时触发并弹出“即将上课”通知，通知带“今天不再提醒/本周暂停”操作。
- 版本升级为 2.14.0（versionCode 93），可直接覆盖升级 2.13.x 及更早正式版本。

## v2.13.0 — 2026-08-21

- 构建栈升级：Gradle 9.1.0、AGP 8.13.0、Kotlin 2.2.0（内置 Compose 编译器插件）、Compose BOM 2025.06.01、core-ktx 1.16.0、lifecycle 2.9.0、activity-compose 1.10.1、coroutines 1.10.1、Apache POI 5.3.0；compileSdk/targetSdk 升至 35。
- 适配 targetSdk 35 强制 edge-to-edge：启用官方 enableEdgeToEdge 入口，移除已弃用的窗口状态栏/导航栏颜色 API，页面栏位视觉与 v2.12.6 保持一致。
- 主 Activity 内联的删除、冲突确认、加密备份导出/导入、备份恢复和粘贴导入弹窗拆分到独立组件文件 CourseDialogs.kt（MainActivity 由 905 行缩减至约 700 行），行为与文案不变。
- 当前分钟刷新（每 30 秒）的订阅从课表屏幕顶层下移到今日摘要、课程卡片和时间线覆盖层内部，分钟跳动不再触发整屏与网格重组。
- 全部 UI 层用户可见文案（约 600 条）从硬编码抽取到 strings.xml，支持统一维护；数据层错误消息保持纯 Kotlin 数据不变。
- 修复恢复写入失败残留的备份临时文件会阻塞后续所有恢复的问题，恢复前自动清理残留并补充故障注入回归测试。
- 新增 2 万课程上限的全量保存/载入性能压测护栏与 2 项备份恢复故障注入测试；数据格式与课表/偏好 schema 均未变化。
- 版本升级为 2.13.0（versionCode 92），可直接覆盖升级 2.12.x 及更早正式版本。

## v2.12.6 — 2026-08-20

- 恢复一级页面之间原有的方向性滑入/滑出动画和周次切换动画。
- 仅课程详情页返回课表一级界面（或日程来源页）继续使用短淡化，避免详情返回时出现夸张缩放和位移。
- 保留系统栏布局修复、深色模式主题底层和自定义背景全屏适配，不改变其他功能与数据格式。
- 版本升级为 `2.12.6`（versionCode 91）。

## v2.12.5 — 2026-08-20

- 全应用统一为 edge-to-edge 坐标系，由各页面 Compose Insets 单独安全避让，修复默认背景、设置等页面被系统与页面重复下推的问题；自定义背景仍完整覆盖状态栏和导航栏。
- 页面和周次切换统一为 100 ms 短交叉淡化，关闭系统预测性返回的窗口缩放，课程详情返回课表不再出现夸张缩放或横向滑动。
- 页面切换底层固定使用当前主题背景色，加载阶段也立即绘制主题底色，修复深色模式动画中透出白色窗口的闪光。
- 清理日程、课程详情和设置页重复或过大的系统栏留白，保留课表末节及底部手势区域的安全间距。
- 版本升级为 `2.12.5`（versionCode 90）；课表和偏好数据格式不变，可直接覆盖升级。

## v2.12.4 — 2026-08-20

- 启用自定义背景时适度提高课程卡片透明度：浅色课程卡片由 82% 调整为 74% 不透明，深色课程卡片由 96% 调整为 86% 不透明，让背景图片更容易看清。
- 未使用自定义背景时继续保持原课程卡片透明度，文字、边框、阴影、布局和点击交互均不改变。
- 版本升级为 `2.12.4`（versionCode 89）。

## v2.12.3 — 2026-08-20

- 课程详情返回主课表或日程列表时改为 150/90 ms 的短淡入淡出，取消大幅横向滑动，返回过程更克制且不干扰原位置感。
- 自定义课表背景启用时让 Compose 画布延伸至状态栏和导航栏，图片及可读遮罩覆盖完整屏幕。
- 顶部内容继续通过系统栏 Insets 安全避让，不会与时间、信号、电量或底部手势区域重叠。
- 默认背景和其他页面仍使用原系统栏布局，不改变设置、详情、导入等页面的可用区域。
- 版本升级为 `2.12.3`（versionCode 88）。

## v2.12.2 — 2026-08-20

- 完成生产源码引用审计，并结合 Kotlin 编译与 Release/R8 使用报告删除 5 个确认无调用的旧入口。
- 删除已被 ViewModel 完整课表管理逻辑替代的仓库删表方法，避免两套删除规则长期并存。
- 删除旧版容错时间解析包装和未接入界面的文本列预览方法，保留当前严格解析、导入和兼容逻辑不变。
- 枚举页面恢复改用缓存的 `entries` 集合，避免重复创建枚举数组；课表数据与偏好格式均未变化。
- 111 项 JVM 测试、16 项 API 34 UI 测试、Release/R8、Lint、签名覆盖升级和连续冷启动全部通过。
- 版本升级为 `2.12.2`（versionCode 87）。

## v2.12.1 — 2026-08-20

- 将课程详情纳入统一页面导航，返回时会回到真实来源页：主课表课程返回主课表，日程课程返回日程列表。
- 主课表每周纵向滚动位置改为可保存状态，进入课程详情、Activity 重建后再返回仍精确停留在原位置。
- 详情页进入编辑后，取消或返回会继续停留在课程详情；保存后按稳定课程系列标识刷新详情。
- 统一定义全部一级、二级、三级页面的返回目标，并新增导航穷举、课表位置恢复、日程来源恢复和详情重建测试。
- 版本升级为 `2.12.1`（versionCode 86）。

## v2.12.0 — 2026-08-20

- 桌面小组件的“今日课程”会按结束时间动态过滤：已结束的课程不再显示，正在进行和尚未开始的课程保留；明日课程不受影响。
- 当前时间红线改在课程卡片之后的顶层绘制，增加随深浅色切换的对比轮廓，穿过正在上课的卡片时仍清晰可见。
- 设置页新增全屏自定义课表背景，通过系统图片选择器授权单张图片，支持预览、更换和恢复默认。
- 背景图会自动约束到 2160 像素长边、校正旧系统图片方向、重编码去除 EXIF/位置元数据，仅保存在应用私有目录，不进入课表备份。
- 自定义背景采用全屏裁切和深浅色自适应可读遮罩，不再增加只改数值但难以感知的透明度开关。
- 再次审计设置页可用性：只读保护下会禁用课表结构、提醒和日历导出等无法安全生效的操作，外观与背景仍可保存；“恢复提醒”仅在存在真实暂停状态时显示。
- 偏好格式升级为 schema 4，课表数据仍为 schema 4；版本升级为 `2.12.0`（versionCode 85）。

## v2.11.0 — 2026-08-20

- 删除只改变课程行高、没有改变信息层级的“课程信息密度”，课表继续按七天、五天和单日视图使用经过验证的固定行高；旧偏好和旧备份中的废弃字段会被安全忽略。
- 删除与“课程名、教室、教师完整显示”目标冲突的小组件隐私开关，不再保留两个互相覆盖的展示规则。
- 桌面小组件默认规格改为 3 列×2 行，并仅允许横向扩展到 4 列和 5 列；3 列仅显示今日课程，4/5 列自动切为左侧今日、右侧明日。
- 小组件顶部始终显示当日日期与星期；课程条目完整保留课程名、教室、教师和时间，不设置省略号或最大行数，今日/明日列表可分别滚动。
- 小组件继续复用统一日期解析规则，支持停课周、整日停课、单课取消、补课、活动课表与归档课表过滤；点击课程可直达详情。
- 优化 RemoteViews 布局测量、深浅色对比度、空数据提示和无障碍朗读，并补充三档宽度、长文本、缺失字段及日期例外回归测试。
- 版本升级为 `2.11.0`（versionCode 84）。

## v2.10.0 — 2026-08-20

- 修复旧版曾将学期起始日保存为周中日期时，升级后日期与星期整体错位的问题；读取旧数据时会自动归一化到该周周一，无需用户重新设置开学日期。
- 停课周选择改为可滚动列表，可正常查看和选择整个学期的所有周次。
- 修复日程列表手势返回直接退出应用，以及设置三级页面返回后丢失二级页面滚动位置的问题；统一各级页面返回路径并保留界面状态。
- 重新整理亮色与深色主题的主色、辅助色和强调色，移除不协调的默认紫色控件，统一为绿色与青绿色视觉体系。
- 重做桌面小组件的信息层级和响应式布局，增加日期、状态、课程名、时间与详情分区，并针对不同组件尺寸自动精简内容。
- 日期计算统一使用单一入口，覆盖课表、日程、提醒、小组件、日期例外与 iCalendar，减少不同功能间的日期规则漂移。
- 版本升级为 `2.10.0`（versionCode 83），补充旧数据迁移、日期解析、小组件展示、返回交互和停课周滚动回归测试。

## v2.9.0 — 2026-08-20

- 新增日程列表，按今天、明天、本周和后续分组，支持课程/教师/教室搜索及课程详情跳转。
- 新增具体日期校历例外：整日停课、单课取消、补课和周标签；课表网格、今日摘要、提醒、小组件与 iCalendar 同步生效。
- 新增每门课程提醒策略，可继承全局设置、单独启停、覆盖提前分钟并选择下课提醒；通知可快捷暂停今天或本周。
- 新增本机最近 10 次自动历史快照、恢复差异预览及恢复前再快照，使用原子文件和路径白名单保护。
- 新增密码加密完整备份，使用 PBKDF2-HMAC-SHA256 与 AES-GCM，错误密码或篡改数据会被拒绝。
- 新增课表复制与归档；归档内容完整保留，但不会参与提醒和小组件，且始终保证至少一张使用中课表。
- 文本粘贴导入支持手动列映射；待确认导入草稿可跨进程恢复，并限制文件、行数和警告数量。
- 新增课程信息密度、增强对比度、减少动画、小组件隐私模式、脱敏诊断报告和桌面快捷方式。
- 小组件支持更小尺寸并可寻找未来 31 天的下一节课；通知和小组件点击可直达对应课程。
- 课表数据升级为 schema 4、偏好升级为 schema 3，继续兼容旧版本；版本升级为 `2.9.0`（versionCode 82）。

## v2.8.0 — 2026-08-20

- 新增完全本地的上课提醒，支持提前 5/10/15/30 分钟、精确闹钟自动降级、重启/升级/时间变化恢复，以及活动课表和旧闹钟二次校验。
- 修复每周仅一节课时提醒链可能在下一周中断、同一时刻多门课程互相取消、跨节课程结束时间错误和 API 26–30 被错误降级的问题。
- 新增桌面小组件，显示今日/下一节课；在状态边界主动刷新并适配系统深浅色。
- 新增停课周、iCalendar 导出和网页课表文本粘贴导入；iCal 使用 CRLF、DTSTAMP、UTF-8 折行并跳过越界或无效事件。
- 修复文本导入误识别中文数字、同一时段不同课程被静默去重、缺省周数固定为 20，以及配置变化后导入确认内容丢失的问题。
- 脱敏副本与完整备份使用独立文件回调，避免系统文件选择器期间 Activity 重建导致导出级别串换。
- 严格备份恢复会拒绝损坏的停课周字段；提醒、Widget、日历和备份数据仍只在本地处理。
- 课表网格改为 Canvas 批量绘制背景与线条，同时保留每个空白节次的 TalkBack 可点击语义。
- 修复深色模式空课表标题不可见，以及停课周多选状态不立即刷新的问题。
- 统一课程详情配色、节次时间处理和数据诊断，并迁移到 Gradle Version Catalog。
- 版本升级为 `2.8.0`（versionCode 81），同步维护文档与发布前审计默认值。

## v2.7.0 — 2026-08-19

- 禁用 Android 系统云备份和设备迁移中的应用数据提取，继续保留应用内手动完整备份与脱敏导出。
- 区分首次启动无数据与主文件/回退副本均损坏；损坏时持续显示只读保护，阻止普通写入。
- 有效 `.bak` 恢复不再被损坏主文件污染；严格校验的完整备份恢复会重建有效主文件与回退副本并解除保护。
- 新增今日/下一节课摘要，覆盖无课、上课中、课间、课程结束、学期外及异常节次时间。
- 新增按课程名、教师和教室的纯本地搜索，清空搜索即可恢复完整显示，不修改课表数据。
- 新逻辑拆分为独立计算模型与 Compose 组件，避免继续扩大主 Activity 和课表网格文件。
- 新增 JVM 回归、无签名 GitHub Actions；API 34 主线运行，API 26/29 每周或手动运行。
- 新增无网络操作的发布前 PowerShell 审计，检查版本、权限、敏感文件忽略规则和 Git 状态。

## v2.6.1 — 2026-08-18

- 为同一课程的跨周拆分记录增加稳定系列标识，修复“应用到全部周”首次删除不完整的问题。
- 全部周编辑会合并同系列的拆分记录，并在冲突检测时正确排除被编辑系列。
- 七天、五天和单日视图改为按课表持久化，应用被系统结束后仍保持用户选择。
- 兼容旧版课表和备份：载入时自动迁移系列标识，旧数据无需手工处理。
- 删除撤销改为定向恢复课程系列，不再覆盖删除后新增或修改的其他课程。
- 优化旧数据迁移的分组性能，并为脱敏副本替换课程系列关联指纹。
- 新增课程系列操作、旧数据迁移、视图持久化与撤销安全测试，并完成强制停止重启回归。

## v2.6.0 — 2026-08-18（未单独发布，功能合并至 v2.6.1）

- 新增按星期、节次和周次交集判断的课程冲突分析。
- 导入预览按新增、合并、重复、冲突分类，安全项默认选择，冲突和重复默认不选。
- 支持筛选预览分类、逐条选择、只选安全项，并展示冲突对象和具体重叠周次/节次。
- 手工新增或编辑课程发生冲突时先二次确认，避免无意覆盖时间段。
- 导入确认界面从 MainActivity 拆分为独立组件，并补充测试语义标记。
- 增加冲突分析、2000 条分析压力、1000 行 XLSX、原子写入中断和 Compose UI 自动化测试。

## v2.5.2 — 2026-08-17

- 七天、五天和单日视图均可完整换行显示课程名、教师和教室信息。
- 优化节次与起止时间布局，并增加底部安全滚动区域，避免最后一节被圆角屏或手势区遮挡。
- 重新布局周次导航和视图切换控件，改善拥挤感与深浅色模式可见度。
- 统一课表、设置、课表管理、课程详情、课程编辑和导入确认界面的设计语言。
- 保留原有多课表、课程编辑、Excel 导入、备份恢复、主题、周切换和本地持久化功能。
- Release 继续启用 R8、资源收缩和语言资源裁剪，不声明网络权限。

## v2.4.0 — 2026-08-08

- 首个公开源码版本。
- 完成数据持久化、导入、备份恢复、错误处理和发布安全加固。
