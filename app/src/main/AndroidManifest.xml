<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 上课提醒：Android 13+ 日历类应用的免授权精确闹钟（自动授予、不可撤销），
         根治 Android 14+ 对 SCHEDULE_EXACT_ALARM 默认拒绝导致的提醒不准时问题。 -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 上课提醒：Android 12 的精确闹钟权限；13+ 由 USE_EXACT_ALARM 覆盖，
         不可用时自动降级为近似闹钟。 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 上课提醒：Android 13+ 通知运行时权限，开启提醒时在应用内请求 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 系统重启后恢复上课提醒；不用于自启动界面或联网。 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 仅“设置 → 检查更新”手动触发时访问 GitHub Releases API；无任何常驻联网行为。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        tools:targetApi="33"
        android:theme="@style/Theme.JaySayCourseTable">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.JaySayCourseTable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".data.reminder.CourseReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".data.reminder.ReminderRestoreReceiver"
            android:directBootAware="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.CourseWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/course_widget_info" />
        </receiver>

        <service
            android:name=".widget.CourseWidgetRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

    </application>

</manifest>
